---
// style import
import "@/styles/starwind.css";
import Navbar from "../components/navbar/navbar.astro";

interface Props {
   noindex?: boolean;
   showNav?: boolean;
}

const { noindex = false, showNav = true } = Astro.props;
---

<!doctype html>
<html lang='en'>
   <head>
      <meta charset='UTF-8' />
      <meta name='viewport' content='width=device-width' />
      <link rel='preconnect' href='https://fonts.googleapis.com' />
      <link rel='preconnect' href='https://fonts.gstatic.com' crossorigin />
      <link
         href='https://fonts.googleapis.com/css2?family=Archivo:ital,wght@0,100..900;1,100..900&family=Host+Grotesk:ital,wght@0,300..800;1,300..800&family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&display=swap'
         rel='stylesheet'
      />
      <link rel='icon' type='image/svg+xml' href='/favicon.svg' />
      <meta name='generator' content={Astro.generator} />
      <meta name='description' content='Agencia de lavaugc.com' />
      <meta name='keywords' content='LAVA, UGC, Agencia, Marketing, RRSS' />
      {noindex && <meta name='robots' content='noindex, nofollow' />}
      <title>LAVA - The UGC Agency</title>
   </head>
   <body>
      {showNav && <Navbar />}
      <slot />
   </body>
</html>

<style>
   /* html {
      font-size: 1.4875rem;
   }
   @media screen and (max-width: 2500px) {
      html {
         font-size: calc(0.0012195121951217303rem + 0.9512195121951221vw);
      }
   }
   @media screen and (max-width: 1680px) {
      html {
         font-size: calc(0.0002902757619738239rem + 0.9521044992743106vw);
      }
   }
   @media screen and (max-width: 991px) {
      html {
         font-size: calc(0.3rem + 0.4vw);
      }
   }
   @media screen and (max-width: 479px) {
      html {
         font-size: calc(0.15rem + 0.35vw);
      }
   } */

   html,
   body {
      margin: 0;
      width: 100%;
      height: 100%;
   }
</style>
